import React from 'react'

const ComponentC = ({name}) => {
  return (
    <div>
      <span>Component C {name} </span><br />
      <span>Name form all the props drilled is : {name}</span>
    </div>
  )
}

export default ComponentC
